# 测试报告

日期：2026-08-15

## 测试环境

- Windows；
- Qt 5.9.7 `mingw53_32`；
- MinGW 5.3.0 32-bit；
- qmake 3.1；
- 构建入口：`scripts/build_and_test.ps1`。

## 构建结果

- 应用 qmake：成功；
- 应用编译与链接：成功；
- 测试 qmake：成功；
- 测试编译与链接：成功；
- QtTest 实际运行：17 passed，0 failed，0 skipped；
- `windeployqt` 打包：成功；
- 打包应用 `--smoke-test`：退出码 0。

## 自动测试覆盖

1. 合法 XML 协议解析；
2. 两个课程子元素式 XML 的元数据、字段和位布局解析；
3. 错误的 `loopEnd = bitIndex + length` 布局被拒绝；
4. 课程字段契约：标识字段必须有固定值、类型长度规则和“不关注字段仍占据报文位区间”；
5. 现有 16 种兼容类型的生成和格式校验；
6. 课程样例 1/2 分别生成 4/12 字节二进制报文，固定头字节为 `12 34`；
7. 课程二进制报文通过真实 UDP Socket 发送和接收，界面预览使用 HEX；
8. 非法字段类型拒绝；
9. 旧格式字段选择、模板和分组输出；
10. SQLite 每次任务汇总写入，以及按时间段、协议、IP 的组合查询；
11. 空协议、非法 IP、端口、频率和数量拒绝；
12. 有限发送完成、持续发送停止后都只产生一条汇总日志，配置数量与实际总数正确；
13. 真实 loopback 基准的序号、接收、异常报文和丢包统计。

## 性能样本

同一次自动测试中的 2000 条 localhost UDP 样本：

- 请求：2000；
- 成功写入：2000；
- 唯一接收：2000；
- 异常报文：0；
- 发送阶段：26.459 ms，约 75,587.50 Hz；
- 端到端：26.546 ms，约 75,339.78 Hz；
- 丢包率：0.00%。

该结果测量的是本机生成器到本机 UDP 接收端的短时基线，不包含常规 GUI 路径的 1–1000 Hz 定时节奏。常规发送只在任务结束时写一条 SQLite 汇总日志。多次验证中的端到端结果会随系统负载波动；它不能直接推广到其他机器、网卡或远端网络。

代码复核修正后又运行一次：2000/2000、异常 0、丢包 0.00%，端到端 53.323 ms，约 37,507.20 Hz。两次吞吐差异进一步说明应以收发数和丢包率为验收主指标。

## 剩余验收风险

- 两个课程协议样例已加入并验证，但它们只出现 `HEX/BIN/STRING/IP/FLAG`；本轮按用户要求不验收最终 14 种类型映射；
- 课程样例未声明位序，当前明确采用网络字节序/MSB-first；
- GUI 已完成无交互冒烟启动，但课程提交前仍建议人工操作一次完整界面流程并保存截图；
- 本次基准是单机单次样本；正式报告若要求统计稳定性，应重复多轮并报告均值、极值和标准差。
