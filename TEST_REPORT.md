# 测试报告

日期：2026-08-08

## 测试环境

- Windows；
- Qt 5.9.7 `mingw53_32`；
- MinGW 5.3.0 32-bit；
- qmake 3.1；
- 构建入口：`scripts/build_and_test.ps1`。

## 构建结果

- 应用 qmake：成功；
- 应用编译与链接：成功；
- 测试 qmake：成功；
- 测试编译与链接：成功；
- QtTest 实际运行：12 passed，0 failed，0 skipped；
- `windeployqt` 打包：成功；
- 打包应用 `--smoke-test`：退出码 0。

## 自动测试覆盖

1. 合法 XML 协议解析；
2. 非法字段类型拒绝；
3. 15 类字段逐类生成和格式校验；
4. 字段选择、模板和分组输出；
5. SQLite 插入及按时间、协议、IP 查询；
6. 空协议、非法 IP、端口、频率和数量拒绝；
7. 有限数量 UDP 发送、localhost 实际接收、进度和日志数量一致；
8. 数量为空时持续发送并由用户停止；
9. 真实 loopback 基准的序号、接收、异常报文和丢包统计。

## 性能样本

同一次自动测试中的 2000 条 localhost UDP 样本：

- 请求：2000；
- 成功写入：2000；
- 唯一接收：2000；
- 异常报文：0；
- 发送阶段：26.506 ms，约 75,455.47 Hz；
- 端到端：26.590 ms，约 75,216.81 Hz；
- 丢包率：0.00%。

该结果测量的是本机生成器到本机 UDP 接收端的短时基线，不包含常规 GUI 路径的 1–1000 Hz 定时节奏和逐条 SQLite 日志开销。多次验证中的端到端结果会随系统负载波动；它不能直接推广到其他机器、网卡或远端网络。

## 剩余验收风险

- 仓库没有老师提供的完整原始题面，当前 15 类字段中的补充类型仍需最终核对；
- GUI 已完成无交互冒烟启动，但课程提交前仍建议人工操作一次完整界面流程并保存截图；
- 本次基准是单机单次样本；正式报告若要求统计稳定性，应重复多轮并报告均值、极值和标准差。
